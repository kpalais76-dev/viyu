/* 1. 页面容器 */
page {
  background-color: #FFFFFF;
  padding-bottom: 150rpx; /* 底部留白，防止内容被按钮遮挡 */
}

.container {
  padding: 40rpx;
}

/* 2. 顶部 Hero 区域 (标题+简介) */
.hero {
  margin-bottom: 50rpx;
  border-bottom: 2rpx solid #F0F2F5;
  padding-bottom: 40rpx;
}

.hero-title {
  font-size: 56rpx;
  font-weight: 900;
  color: #1A1D26;
  margin-bottom: 20rpx;
  letter-spacing: 2rpx;
  line-height: 1.2;
}

.hero-sub {
  font-size: 30rpx;
  color: #666;
  line-height: 1.6;
  text-align: justify;
}

.hero-tag {
  display: inline-block;
  background: #1A1D26;
  color: #FFF;
  padding: 8rpx 18rpx;
  border-radius: 8rpx;
  font-size: 24rpx;
  font-weight: bold;
  margin-top: 10rpx;
}

/* 3. 🧠 智能分析面板 (核心联动功能) */
.smart-panel {
  background: #1A1D26; /* 深色背景 */
  color: #FFFFFF;
  padding: 30rpx;
  border-radius: 20rpx;
  margin-bottom: 50rpx;
  box-shadow: 0 10rpx 30rpx rgba(26, 29, 38, 0.2);
}

.sp-title {
  font-size: 22rpx;
  color: rgba(255, 255, 255, 0.6);
  margin-bottom: 24rpx;
  letter-spacing: 2rpx;
  font-weight: bold;
  text-transform: uppercase;
}

.sp-grid {
  display: flex;
  gap: 24rpx; /* 栅格间距 */
}

.sp-item {
  flex: 1;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 12rpx;
  padding: 24rpx;
  text-align: center;
  position: relative;
  border: 2rpx solid transparent; /* 默认边框透明 */
}

/* 状态：OK (绿色) */
.sp-item.ok {
  border-color: #00E676;
  background: rgba(0, 230, 118, 0.05);
}

/* 状态：NO (红色) */
.sp-item.no {
  border-color: #FF3B30;
  background: rgba(255, 59, 48, 0.05);
}

.sp-val {
  font-size: 40rpx;
  font-weight: 900;
  display: block;
  margin-bottom: 8rpx;
  font-family: monospace; /* 数字字体 */
}

.sp-lbl {
  font-size: 22rpx;
  opacity: 0.8;
}

.sp-icon {
  position: absolute;
  top: 12rpx;
  right: 12rpx;
  font-size: 24rpx;
}

.sp-tip {
  font-size: 22rpx;
  color: #FF9800; /* 橙色警告 */
  margin-top: 24rpx;
  padding-top: 20rpx;
  border-top: 2rpx solid rgba(255, 255, 255, 0.1);
  line-height: 1.4;
}

/* 4. 通用内容区块 */
.section {
  margin-bottom: 50rpx;
}

.sec-title {
  font-size: 26rpx;
  font-weight: 900;
  color: #2962FF;
  margin-bottom: 24rpx;
  text-transform: uppercase;
  letter-spacing: 1rpx;
  display: flex;
  align-items: center;
}

/* 详情行 (用于钓法详情) */
.detail-row {
  font-size: 30rpx;
  color: #333;
  margin-bottom: 20rpx;
  line-height: 1.6;
  background: #F8F9FB;
  padding: 20rpx;
  border-radius: 12rpx;
}

.detail-row .label {
  font-weight: bold;
  color: #1A1D26;
  margin-right: 10rpx;
}

/* 步骤列表 (用于手法详情) */
.step-list {
  padding-left: 10rpx;
}

.step-item {
  margin-bottom: 24rpx;
  font-size: 30rpx;
  color: #333;
  line-height: 1.6;
  padding-left: 20rpx;
  border-left: 6rpx solid #E0E0E0; /* 左侧装饰线 */
}

/* 提示框 */
.tips-box {
  background: #FFF3E0;
  color: #E65100;
  padding: 30rpx;
  border-radius: 12rpx;
  font-size: 28rpx;
  font-weight: bold;
  line-height: 1.5;
  border: 2rpx solid #FFE0B2;
}

/* 5. 底部悬浮按钮栏 */
.footer-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  background: #FFFFFF;
  padding: 20rpx 40rpx 50rpx 40rpx; /* 底部多留白适配全面屏 */
  box-shadow: 0 -8rpx 30rpx rgba(0, 0, 0, 0.05);
  z-index: 100;
}

.link-btn {
  background: #1A1D26;
  color: #FFFFFF;
  font-weight: 900;
  height: 100rpx;
  line-height: 100rpx;
  border-radius: 50rpx;
  box-shadow: 0 10rpx 20rpx rgba(26, 29, 38, 0.3);
  font-size: 32rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border: none;
}

.link-btn:active {
  transform: scale(0.98);
  opacity: 0.9;
}