<view class="gear-page">
  
  <view class="gear-header">
    <view class="search-bar">
      <text class="search-icon">🔍</text>
      <input class="search-input" placeholder="搜索我的装备..." disabled />
    </view>
    <view class="inventory-stat">
      <text>库存总值: ¥{{totalValue}}</text>
      <text>单品: {{totalCount}}</text>
    </view>
  </view>

  <view class="segment-wrapper">
    <view class="segment-bar">
      <view class="segment-item active">我的库存</view>
      <view class="segment-item">套装方案</view>
    </view>
  </view>

  <view class="gear-list">
    
    <view class="add-new-card" bindtap="handleAddGear">
      <view class="dashed-box">
        <text class="plus-icon">+</text>
        <text>点击模拟录入新装备</text>
      </view>
    </view>

    <block wx:if="{{gearList.length > 0}}">
      <view class="gear-card" wx:for="{{gearList}}" wx:key="id">
        <view class="gear-img-box {{item.category === 'rod' ? 'rod' : 'reel'}}">
          <text>{{item.category === 'rod' ? '🎣' : '⚙️'}}</text>
        </view>
        
        <view class="gear-info">
          <view class="gear-title-row">
            <text class="gear-name">{{item.brand}} {{item.name}}</text>
            <text class="gear-badge">{{item.category === 'rod' ? '鱼竿' : '渔轮'}}</text>
          </view>
          <view class="gear-params">
            <text>{{item.param}}</text>
          </view>
          <view class="gear-meta">
            <text class="date">{{item.date}} 入手</text>
          </view>
        </view>
      </view>
    </block>

    <view wx:else class="empty-tip">
      <text>暂无装备，请点击上方“+”添加</text>
    </view>

  </view>

  <view style="height: 120rpx;"></view>
</view>